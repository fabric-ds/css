name: Deploy to GitHub Pages

on: [push]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
            yarn
      - name: Build Packages
        run: |
            yarn build
